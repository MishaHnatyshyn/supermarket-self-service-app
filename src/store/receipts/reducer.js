const initialState = {
  receipts: [],
  isLoading: false,
  isError: false,
}

export default function receiptsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
